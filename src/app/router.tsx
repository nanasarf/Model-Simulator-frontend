import { useEffect } from 'react'; import { Navigate, Route, Routes, useLocation } from 'react-router-dom'; import { Protected, LoginPage, RegisterPage, InstructorRegistrationPage, Forbidden, homeFor } from '../features/auth/pages'; import { Shell } from './shell'; import { useAuth } from './runtime'; import { InstructorHome } from '../features/instructor/home'; import { ClassroomListPage, ClassroomDetailPage } from '../features/instructor/classrooms'; import { InstructorSessionsPage, SessionSetupPage } from '../features/instructor/sessions'; import { CreateScenarioPage, ScenarioLibraryPage, ScenarioVersionHistoryPage, PublishedVersionPage } from '../features/scenarios/library'; import { ScenarioEditorPage } from '../features/scenarios/editor'; import { AiScenarioAuthoringPage } from '../features/scenarios/ai'; import { StudentHome, SessionsPage } from '../features/student/home'; import { SimulationWorkspace } from '../features/simulation/workspace';
function HomeRedirect(){return <Navigate to={homeFor(useAuth().user)} replace/>} function RouteFocus(){const{pathname}=useLocation();useEffect(()=>{document.getElementById('main')?.focus();window.scrollTo?.(0,0)},[pathname]);return null}
export function AppRoutes(){return <><RouteFocus/><Routes><Route path="/auth/register" element={<RegisterPage/>}/><Route path="/auth/instructor/register" element={<InstructorRegistrationPage/>}/><Route path="/auth/*" element={<LoginPage/>}/><Route element={<Protected/>}><Route path="/forbidden" element={<Forbidden/>}/><Route element={<Shell/>}><Route index element={<HomeRedirect/>}/><Route element={<Protected role="Instructor"/>}><Route path="/instructor" element={<InstructorHome/>}/><Route path="/instructor/classrooms" element={<ClassroomListPage/>}/><Route path="/instructor/classrooms/:classroomId" element={<ClassroomDetailPage/>}/><Route path="/instructor/scenarios" element={<ScenarioLibraryPage/>}/><Route path="/instructor/scenarios/create" element={<CreateScenarioPage/>}/><Route path="/instructor/scenarios/ai/new" element={<AiScenarioAuthoringPage/>}/><Route path="/instructor/scenarios/versions/:versionId" element={<PublishedVersionPage/>}/><Route path="/instructor/scenarios/:scenarioId/versions" element={<ScenarioVersionHistoryPage/>}/><Route path="/instructor/scenarios/:model/:draftId" element={<ScenarioEditorPage/>}/><Route path="/instructor/sessions" element={<InstructorSessionsPage/>}/><Route path="/instructor/sessions/:sessionId/setup" element={<SessionSetupPage/>}/></Route><Route element={<Protected role="Student"/>}><Route path="/student" element={<StudentHome/>}/><Route path="/student/sessions" element={<SessionsPage/>}/></Route><Route path="/simulation/:sessionId" element={<SimulationWorkspace/>}/><Route path="*" element={<section className="state"><h1>Page not found</h1><p>Choose a workspace from the navigation.</p></section>}/></Route></Route></Routes></>}
